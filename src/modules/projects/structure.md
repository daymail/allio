[WALLWATCH]
- palette view (scheme.json) the file view, and color grid blocks [bottom right section]
- corresponding image to the generated palette
- interactive terminal section
- process graphs & timer in ms
- file structure tree
- small git section (not something complex, just if it has git or not, if it's pushed and others)
- notifications
- IPC log messages section
- cache storage tree paired with the respective wallpaper
- wallpaper theme generator (shows a list of wallpapers, and to the right the wallpaper itself)
- history (timestamps of palette generation) <-part of the main area section
- theme metadata section [Author, version, last edited timestamp, github links, social links]
- small command line section that lists clickable commands [Export, Redo, Settings, Quit, Help, Generate Palette from base color, Live Preview, Full screen Palette View, Search Tokens]
- Dark mode / Light Mode

*HEADER*
WALLWATCH
- description
- if process is running or not
- status ACTIVE/ INACTIVE
- terminal name (for capabilities)
- few key bind shortcuts for essential commands (h-help(keybind list)) - as the titles at bottom left
- daemon (if daemon is running)
- git branch
*for active/inactive items, set them to colors that are on or off*





[SCOUTD]
- log message section
